package-lock.json
prisma/migrations
*.md

# Build output and dependencies. Without these, `format:check` fails on ~221
# generated files whenever it runs after a build (CI only passed because it
# runs format:check before build).
.next/
node_modules/

# Supabase writes generated edge-runtime sources here on `supabase start`. Git
# already ignores the directory, so CI never sees it -- but locally it makes
# format:check fail on a file nobody wrote.
supabase/.temp/
